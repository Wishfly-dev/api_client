name: wishfly_api_client
description: A starting point for Dart libraries or applications.
version: 0.1.0

environment:
  sdk: ^3.0.0

dependencies:
  http: ^0.13.3
  wishfly_shared: ^0.1.3+1

dev_dependencies:
  lints: ^2.0.0
  test: ^1.21.0
